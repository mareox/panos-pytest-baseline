[project]
name = "panos-pytest-baseline"
version = "0.1.0"
description = "Pytest-based PAN-OS firewall baseline control suite"
requires-python = ">=3.10"
dependencies = [
    "pytest",
    "requests",
    "pyyaml",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
